; Design-system is installed from GitHub over HTTPS (see package-lock.json).
; If alocare-design-system is private, add in Vercel: GITHUB_TOKEN=<PAT with repo read>
; and uncomment the next line:
; //github.com/:_authToken=${GITHUB_TOKEN}
